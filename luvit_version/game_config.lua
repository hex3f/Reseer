-- 游戏配置 (外部可修改)
-- 包含：初始玩家数据、初始精灵、系统公告等
-- 所有配置项都会被代码读取，不应该有硬编码

local Config = {}

-- ============================================================
-- 初始玩家数据 (新用户默认值)
-- ============================================================
Config.InitialPlayer = {
    -- ============================================================
    -- 基础属性
    -- ============================================================
    regTime = 0,          -- 注册时间戳 (0=使用当前时间)
    
    -- 货币
    coins = 2000,         -- 初始赛尔豆
    gold = 0,             -- 初始金豆
    
    -- 外观与标识
    color = 0x66CCFF,     -- 默认颜色
    texture = 1,          -- 材质
    dsFlag = 0,           -- 黑暗/特殊标识
    badge = 0,            -- 徽章ID
    curTitle = 0,         -- 当前称号
    
    -- 初始位置
    mapID = 1,            -- 初始地图ID (1=机械室/飞船)
    posX = 300,           -- X坐标
    posY = 270,           -- Y坐标
    
    -- 属性
    energy = 100,         -- 初始能量/体力
    fightBadge = 0,       -- 战斗徽章
    
    -- ============================================================
    -- 时间/防沉迷
    -- ============================================================
    timeToday = 0,        -- 今日在线时间
    timeLimit = 86400,    -- 每日时间限制 (秒), 86400=24小时=不限制
    
    -- 半天/特殊活动标识 (0或1)
    isClothHalfDay = 0,
    isRoomHalfDay = 0,
    iFortressHalfDay = 0,
    isHQHalfDay = 0,
    
    -- ============================================================
    -- 社交计数
    -- ============================================================
    loginCnt = 1,         -- 登录次数
    inviter = 0,          -- 邀请人ID
    newInviteeCnt = 0,    -- 新邀请数量
    
    -- ============================================================
    -- 师徒系统
    -- ============================================================
    teacherID = 0,
    studentID = 0,
    graduationCount = 0,
    maxPuniLv = 100,
    
    -- ============================================================
    -- 关卡与战斗
    -- ============================================================
    curStage = 0,
    maxStage = 0,
    curFreshStage = 0,
    maxFreshStage = 0,
    maxArenaWins = 0,
    monKingWin = 0,       -- 精灵王获胜次数
    monBtlMedal = 0,      -- 战斗奖牌
    messWin = 0,          -- 乱斗胜利次数
    
    -- 战斗辅助倍率
    twoTimes = 0,         -- 双倍经验次数
    threeTimes = 0,       -- 三倍经验次数
    autoFight = 0,        -- 自动战斗开关
    autoFightTimes = 0,   -- 自动战斗次数
    energyTimes = 0,      -- 能量倍率次数
    learnTimes = 0,       -- 学习倍率次数
    
    -- ============================================================
    -- 精灵相关
    -- ============================================================
    petMaxLev = 100,      -- 精灵最大等级
    petAllNum = 0,        -- 精灵总数记录
    
    -- ============================================================
    -- NoNo 系统配置 (VIP/超能NoNo 统一在此配置)
    -- 所有 NONO 和 VIP 相关配置都在这里
    -- ============================================================
    nono = {
        -- ========== 基础状态 (参照官服默认值) ==========
        hasNono = 1,          -- 是否拥有NoNo (1=有, 0=无)
        flag = 1,             -- NoNo 功能标志位
        state = 0,            -- NoNo状态 (0=关闭/跟随, 1=开启/驻守)
        nick = "NONO",        -- NoNo昵称 (最长16字符) - 官服默认大写
        color = 0xFFFFFF,     -- NoNo颜色 - 官服默认白色
        
        -- ========== VIP/超能NoNo (合并原 isVIP 相关配置) ==========
        -- 这里的配置等同于原来的 isVIP, vipLevel, vipStage 等
        superNono = 0,        -- 是否超能NoNo/VIP (1=是VIP, 0=普通用户)
        vipLevel = 0,         -- VIP等级 (0-10)
        vipStage = 0,         -- VIP阶段 (0-6) - 官服默认0
        autoCharge = 0,       -- 自动续费标识 (0=关闭, 1=开启)
        vipEndTime = 0,       -- VIP过期时间戳 (0=永久或未开通)
        vipValue = 0,         -- VIP经验值
        freshManBonus = 0,    -- 新手奖励标识
        
        -- ========== 超能属性 ==========
        superEnergy = 0,      -- 超能能量 (0-99999)
        superLevel = 0,       -- 超能等级 (0-10)
        superStage = 0,       -- 超能阶段 (0-4) - 官服默认0
        
        -- ========== NoNo 属性值 (参照官服默认值) ==========
        -- 注意: 体力和心情的实际值 = 显示值 / 1000
        -- 例如 power=10000 表示 10.0% 体力
        power = 10000,        -- 体力 (0-100000) - 官服默认10000 (10%)
        mate = 10000,         -- 心情 (0-100000) - 官服默认10000 (10%)
        iq = 0,               -- 智力 (0-100)
        ai = 0,               -- AI数值 (0-100)
        hp = 100000,          -- 当前生命值
        maxHp = 100000,       -- 最大生命值
        energy = 100,         -- 电池能量 (0-100)
        
        -- ========== 时间相关 ==========
        birth = 0,            -- 出生时间戳 (0=使用当前时间)
        chargeTime = 500,     -- 充电时间 - 官服默认500
        expire = 0,           -- SuperNoNo过期时间
        
        -- ========== 其他 ==========
        chip = 0,             -- 芯片数量
        grow = 0,             -- 成长值
        isFollowing = false,  -- 是否正在跟随 (布尔值)
        
        -- ========== 超能NONO开通配置 ==========
        superNonoDurationDays = 30   -- 超能NONO默认开通天数 (30=一个月)
    }
}

-- ============================================================
-- 默认精灵属性 (当系统给予精灵或数据缺失时的默认值)
-- ============================================================
Config.PetDefaults = {
    level = 5,      -- 默认等级
    
    -- 使用函数来实现动态随机 (每次获取时都会随机生成)
    -- 如果想要固定值，可以直接写数字，例如: dv = 31
    dv = function() return math.random(0, 31) end,         -- 随机个体值 (0-31)
    nature = function() return math.random(0, 24) end,     -- 随机性格 (0-24)
}

-- ============================================================
-- 系统公告 (玩家登录后发送)
-- ============================================================
Config.SystemMessage = {
    enabled = true,  -- 是否启用开场公告
    
    -- 公告内容 (支持多行)
    content = [[
欢迎来到赛尔号怀旧服！
这里是您的私人宇宙空间。
祝您游戏愉快！
]]
}

return Config
